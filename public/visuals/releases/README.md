# Release cover assets

Release covers are currently **code-native** via the `ReleaseCover` component (`components/ReleaseCover.tsx`).

Future raster release covers may be added here. Update `ReleaseCover` or release metadata when custom cover art is available.

Open Graph imagery is generated by `app/opengraph-image.tsx`.
